public interface FazMetros {
    // as classes abstratas sao usadas para hieraquias normalmente

    void setPointsPerMeter(int points);

    int getPointsPerMeter();

    double computePoints();
}